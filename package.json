{
  "name": "metrorider-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd streets-gl && npm run dev:server & cd streets-gl && npm run dev",
    "build": "cd streets-gl && npm install --include=dev && npm run build:all",
    "start": "node streets-gl/seed-data.js && node streets-gl/server/dist/index.js"
  }
}
